apiVersion: v1
kind: Service
metadata:
  name: {{ include "nestjs-realworld-exmaple-app.fullname" . }}
  labels:
    {{- include "nestjs-realworld-exmaple-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nestjs-realworld-exmaple-app.selectorLabels" . | nindent 4 }}
