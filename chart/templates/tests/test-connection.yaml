apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nestjs-realworld-exmaple-app.fullname" . }}-test-connection"
  labels:
    {{- include "nestjs-realworld-exmaple-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nestjs-realworld-exmaple-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
